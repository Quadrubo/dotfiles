print(get_process_name());

if (get_process_name() == 'Discord') then
  set_window_workspace(9);
  maximize();
end

if (get_process_name() == 'spotify') then
  set_window_workspace(10);
  maximize();
end

if (get_process_name() == 'signal-desktop') then
  set_window_workspace(8);
  maximize();
end
